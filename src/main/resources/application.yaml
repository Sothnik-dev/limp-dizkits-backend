spring:
  application:
    name: limp-dizkits
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:postgresql://localhost:5433/limp-dizkits
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver